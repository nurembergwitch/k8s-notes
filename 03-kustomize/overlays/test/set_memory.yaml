apiVersion: apps/v1
kind: Deployment
metadata:
  name: mywebapp
spec:
  template:
    spec: 
      containers:
      - name: mycontainer
        resources:
          limits:
            memory: 512Mi
            #cpu: 1/2

